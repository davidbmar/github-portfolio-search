---

## Summary

### Files changed
- **`src/ghps/export.py`** (new) — Static JSON export module with `export_static_bundle()` producing `repos.json`, `clusters.json`, `search-index.json`
- **`src/ghps/cli.py`** (modified) — Added `ghps export --db PATH --output DIR` command
- **`tests/test_export.py`** (new) — 24 tests covering repos/clusters/search-index generation, CLI integration, empty store edge case

### Commands run
- `python3 -m pytest tests/ -v` — **123 passed** (99 existing + 24 new), 0 failures
- `git push -u origin HEAD` — pushed to `agentA-data-export`

### Notes / follow-on work
- **B-005 and B-006** were already fixed in Sprint 3 — all 99 pre-existing tests pass without changes
- The `search-index.json` uses a keyword-based approach (extracted tokens from chunk text + metadata) for client-side search. If agentB's web UI needs embedding-based search instead, the format can be extended later
- `clusters.json` omits centroids (large float arrays) to keep the payload lightweight for the browser
