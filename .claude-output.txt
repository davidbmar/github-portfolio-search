All tasks complete.

---

**Files changed:**
- `deploy.sh` (new) — idempotent deployment script
- `web/data/.gitkeep` (new) — data directory placeholder
- `web/health.json` (new) — health check endpoint
- `.gitignore` — added `_build/` exclusion
- `README.md` — added Deployment section
- `docs/project-memory/sessions/S-2026-03-22-0606-sprint4-deploy-pipeline.md` (new) — session doc

**Commands run:**
- `python3 -m pytest tests/ -v` — 99 passed
- `git commit` + `git push -u origin HEAD`

**Notes / follow-on work:**
- `deploy.sh` gracefully handles missing `ghps export` command (owned by agentA) — once agentA implements `export.py`, the deploy script will automatically pick it up
- The `--output-dir` flag passed to `ghps export` may need adjustment depending on agentA's CLI design
- After sprint merge, run an actual deploy to verify AWS credentials and S3/CloudFront connectivity
